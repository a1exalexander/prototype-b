const state = {
  types: [
    'Строка',
    'Число',
    'Пользовательский',
  ],
};

const mutations = {
  
};

const getters = {
 
};

const actions = {
  
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
